import { createEslintConfig } from '@webikon/webentor-configs/eslint';

// Keep consumer-level overrides small; shared defaults live in @webikon/webentor-configs.
export default createEslintConfig();
